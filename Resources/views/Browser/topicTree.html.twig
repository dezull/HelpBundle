<ul class="category">
    {% for category, topics in tree %}
        <li>
            <h4>{{ category }}</h4>
            <ul class="topic">
                {% for topic in topics %}
                    {% if selectTopicId == topic.id %}
                        <li class="current">{{ topic.title }}</li>
                    {% else %}
                        <li><a href="{{ path('dezull_help_browser_index', {title: topic.title }) }}">{{ topic.title }}</a></li>
                    {% endif %}
                {% endfor %}
            </ul>
        </li>
    {% endfor %}
</ul>
